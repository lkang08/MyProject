<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/iv_gift"
        android:layout_width="@dimen/room_animator_gift_size"
        android:layout_height="@dimen/room_animator_gift_size"
        android:scaleType="fitCenter" />

    <TextView
        android:id="@+id/tv_weekstar_tile"
        android:layout_width="wrap_content"
        android:layout_height="10dp"
        android:background="@drawable/weekstar_tile_tag"
        android:textColor="#362e2b"
        android:textSize="8sp"
        android:paddingBottom="1dp"
        tools:text="ME小伙伴"
        android:visibility="gone"
        tools:visibility="visible" />
</LinearLayout>