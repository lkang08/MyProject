<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TestTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="NorProgressCircle">
        <item name="android:indeterminateDrawable">@drawable/bd_progress_rotate</item>
        <item name="android:minWidth">25dp</item>
        <item name="android:minHeight">25dp</item>
        <item name="android:maxWidth">60dp</item>
        <item name="android:maxHeight">60dp</item>
    </style>

    <declare-styleable name="CornersGifView">
        <attr name="leftBottomCorner" format="dimension"></attr>
        <attr name="leftTopCorner" format="dimension"></attr>
        <attr name="rightBottomCorner" format="dimension"></attr>
        <attr name="rightTopCorner" format="dimension"></attr>
        <attr name="cornerSize" format="dimension"></attr>
    </declare-styleable>

    <style name="AppCompact.Theme.Transparent.FullScreen" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

</resources>
